<h2 class="mb-4 fw-bold"><%= @temple.name %></h2>
<p class="text-muted"><%= @temple.description %></p>

<h3 class="mt-5 mb-3 fw-bold">Available Pujas</h3>
<div class="row g-4">
  <% @pujas.each do |puja| %>
    <div class="col-md-4">
      <div class="card h-100 shadow-sm">
        <div class="card-body d-flex flex-column">
          <h5 class="card-title fw-bold"><%= puja.title %></h5>
          <p class="card-text text-truncate"><%= puja.description %></p>
          <span class="badge bg-success mb-2">₹ <%= puja.base_price %></span>
          <%= link_to "Book Now", new_puja_booking_path(puja), class: "btn btn-success mt-auto" %>
        </div>
      </div>
    </div>
  <% end %>
</div>
