

<!-- 🔹 Hero Section -->
<section class="hero text-center text-white py-4" style="background: url('/assets/puja-hero.jpg') center/cover no-repeat; position: relative;">
  <!-- ✨ Divine Overlay -->
  <div style="background: rgba(255, 136, 0, 0.25); position: absolute; inset: 0;"></div>

  <!-- 🌸 Content -->
  <div class="container position-relative">
    <h1 class="fw-bold mb-2" style="font-size: 1.8rem; color: #ffb300; text-shadow: 0 0 10px rgba(255, 187, 0, 0.6); font-family: 'Poppins', sans-serif;">
      🕉 Book Your Pooja
    </h1>
    <p class="mb-3" style="color: #fff8e7; font-size: 0.95rem; font-family: 'Noto Serif', serif; text-shadow: 0 0 6px rgba(255, 175, 0, 0.6);">
      Invoke divine blessings through sacred rituals — book your Online or Offline Pooja now.
    </p>
    <a href="#book-now" 
       style="display: inline-block; color: #ffb300; font-weight: 600; text-decoration: none; font-family: 'Poppins', sans-serif; font-size: 1rem; text-shadow: 0 0 8px rgba(255, 187, 0, 0.6); transition: all 0.3s ease;">
      🙏 Book Now
    </a>
  </div>
</section>

<style>
  @import url('https://fonts.googleapis.com/css2?family=Noto+Serif:wght@400;600&family=Poppins:wght@500;700&display=swap');

  .hero {
    position: relative;
    overflow: hidden;
  }

  .hero a:hover {
    color: #ffd85e;
    text-shadow: 0 0 12px rgba(255, 200, 50, 0.9);
    transform: scale(1.05);
  }

  @media (max-width: 768px) {
    .hero h1 {
      font-size: 1.4rem !important;
    }
    .hero p {
      font-size: 0.85rem !important;
    }
    .hero a {
      font-size: 0.9rem !important;
    }
  }
</style>




<!-- 🔹 Pujas Cards -->
<section class="puja-list py-5">
  <div class="container">
    <% if @pujas.present? %>
      <div class="row g-4">
        <% @pujas.each do |puja| %>
          <div class="col-lg-4 col-md-6">
            <div class="card h-100 shadow-sm border-0 rounded-3 overflow-hidden">
              <% if puja.image.attached? %>
                <%= image_tag puja.image, class: "card-img-top", style: "height:200px; object-fit:cover;" %>
              <% else %>
                <img src="https://via.placeholder.com/300x200" class="card-img-top">
              <% end %>

              <div class="card-body d-flex flex-column">
                <h5 class="card-title fw-bold mb-1"><%= puja.title %></h5>
                <p class="card-text text-muted mb-2" style="font-size: 0.9rem;"><%= truncate(puja.description, length: 100) %></p>
                <div class="mt-auto d-flex justify-content-between align-items-center">
                  <span class="fw-bold">₹<%= puja.base_price %></span>
                  
                  <!-- 🟠 Orange "Book" Button -->
                  <%= link_to "Book", puja_path(puja),
                      class: "btn btn-sm rounded-2",
                      style: "background-color: #ff7b00; color: white; font-weight: 600; border: none; transition: all 0.3s ease;",
                      onmouseover: "this.style.backgroundColor='#ff8c1a';",
                      onmouseout:  "this.style.backgroundColor='#ff7b00';"
                  %>
                </div>
              </div>
            </div>
          </div>
        <% end %>
      </div>
    <% else %>
      <p class="text-center text-muted mt-4">🙏 No Pujas available right now.</p>
    <% end %>
  </div>
</section>


<!-- 🔹 CSS -->
<style>
  /* Top Bar */
  .top-bar {
    width: 100%;
    background-color: #f0501e;
    color: white;
    font-size: 14px;
  }

  .top-bar .top-bar-content {
    max-width: 1200px;
    margin: 0 auto;
    display: flex;
    justify-content: space-between;
    align-items: center;
    padding: 5px 20px;
  }

  .top-bar .left, .top-bar .right {
    display: flex;
    align-items: center;
    gap: 15px;
  }

  .top-bar a {
    color: white;
    text-decoration: none;
  }

  .language-button {
    background: #ffe0b2;
    color: #000;
    padding: 3px 8px;
    border-radius: 5px;
    font-size: 13px;
  }

  /* Header */
  header {
    width: 100%;
    background: white;
    box-shadow: 0 2px 6px rgba(0,0,0,0.1);
  }

  .header-content {
    max-width: 1200px;
    margin: 0 auto;
    display: flex;
    justify-content: space-between;
    align-items: center;
    padding: 10px 20px;
  }

  header .logo {
    display: flex;
    align-items: center;
    gap: 10px;
  }

  nav a {
    margin: 0 12px;
    text-decoration: none;
    color: #333;
    font-weight: bold;
  }

  nav a:hover, nav a.active {
    color: #f44300;
  }

  /* Hero Section */
  .hero {
    position: relative;
    padding: 100px 0;
  }

  .hero h1 {
    font-size: 2.5rem;
    z-index: 2;
    position: relative;
  }

  .hero p {
    font-size: 1.1rem;
    position: relative;
    z-index: 2;
  }

  /* Pujas Cards */
  .card:hover {
    transform: translateY(-3px);
    transition: all 0.2s ease;
    box-shadow: 0 10px 15px rgba(0,0,0,0.15);
  }

  /* Responsive */
  @media (max-width: 768px) {
    .header-content, .top-bar-content {
      flex-direction: column;
      align-items: flex-start;
      gap: 10px;
    }

    nav a {
      margin: 0 8px 0 0;
    }

    .hero h1 {
      font-size: 2rem;
    }

    .hero p {
      font-size: 1rem;
    }
  }
</style>

<script src="https://kit.fontawesome.com/a076d05399.js" crossorigin="anonymous"></script>
