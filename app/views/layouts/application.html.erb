<!DOCTYPE html>
<html>
<head>
  <title>Ujjain Online Pooja Booking</title>
  <%= csrf_meta_tags %>
  <%= csp_meta_tag %>
  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet">
  <style>
    body { background-color: #f5f5f5; }
    .card:hover { transform: translateY(-5px); box-shadow: 0 10px 20px rgba(0,0,0,0.2); transition: 0.3s; }
    .temple-card img { height: 200px; object-fit: cover; }
    .btn-primary, .btn-success { border-radius: 50px; padding: 0.5rem 1.5rem; }
    .navbar { box-shadow: 0 4px 6px rgba(0,0,0,0.1); }
    footer { background: #343a40; color: white; padding: 20px 0; margin-top: 40px; text-align: center; }
  </style>
</head>
<body>
<nav class="navbar navbar-expand-lg navbar-dark bg-dark">
  <div class="container">
    <a class="navbar-brand fw-bold" href="/">🕉 Ujjain Pooja Booking</a>
  </div>
</nav>

<div class="container my-5">
  <% if notice %>
    <div class="alert alert-success shadow-sm"><%= notice %></div>
  <% end %>
  <%= yield %>
</div>

<footer>
  &copy; <%= Time.current.year %> Ujjain Pooja Booking. All rights reserved.
</footer>
</body>
</html>
